spring:
  config:
    import: "configserver:"
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: chat-config-cloud
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka